# Architecture

WatchdogX uses multi-agent collaboration orchestrated by a DAG workflow engine.

## Components
- Orchestrator: DAG workflow engine
- MemoryStore: vector memory with semantic search
- ToolRegistry: tool registry for LLM Function Calling
